rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /feedbacks/{feedback} {
      allow read, write: if true;  // Temporairement pour le développement
    }
  }
}